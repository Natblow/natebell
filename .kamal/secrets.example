# Copy to .kamal/secrets and fill in your Docker Hub (or registry) credentials.
# .kamal/secrets is git-ignored — never commit real values.

KAMAL_REGISTRY_PASSWORD=your_registry_password_or_access_token
